{
  "name": "legal-platform-workspace",
  "private": true,
  "description": "5173=师生门户(frontend)，5174=管理后台(admin)，请分别启动",
  "scripts": {
    "dev:portal": "npm run dev --prefix ./frontend",
    "dev:admin": "npm run dev --prefix ./admin",
    "build:portal": "npm run build --prefix ./frontend",
    "build:admin": "npm run build --prefix ./admin"
  }
}
